@model yoklamaWeb.ViewModels.Pansiyon.NobetciPlanEditViewModel
@{
    ViewData["Title"] = "Nöbet Planı Düzenle";
    Layout = "_Layout";
}

<div class="container mt-4" style="max-width: 600px;">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-warning text-dark">
            <h4 class="mb-0 fw-bold">Nöbet Planı Düzenle</h4>
        </div>
        <div class="card-body p-4">
            <form method="post" asp-action="Edit">
                @Html.AntiForgeryToken()
                
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="RowVersion" />

                <div class="mb-3">
                    <label asp-for="PansiyonGorevlisiId" class="form-label fw-bold">Nöbetçi Görevli</label>
                    <select asp-for="PansiyonGorevlisiId" asp-items="ViewBag.Gorevlisiler" class="form-select"></select>
                    <span asp-validation-for="PansiyonGorevlisiId" class="text-danger"></span>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="BaslangicTarihi" class="form-label fw-bold"></label>
                        <input asp-for="BaslangicTarihi" type="date" class="form-control" />
                        <span asp-validation-for="BaslangicTarihi" class="text-danger"></span>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label asp-for="BitisTarihi" class="form-label fw-bold"></label>
                        <input asp-for="BitisTarihi" type="date" class="form-control" />
                        <span asp-validation-for="BitisTarihi" class="text-danger"></span>
                    </div>
                </div>

                <div class="mb-3">
                    <label asp-for="Gun" class="form-label fw-bold">Nöbet Günü</label>
                    <select asp-for="Gun" asp-items="ViewBag.Gunler" class="form-select"></select>
                    <span asp-validation-for="Gun" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Aciklama" class="form-label fw-bold"></label>
                    <textarea asp-for="Aciklama" class="form-control" rows="3"></textarea>
                    <span asp-validation-for="Aciklama" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Index" class="btn btn-secondary">Vazgeç</a>
                    <button type="submit" class="btn btn-warning fw-semibold">Güncelle</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
